import React from "react";

type Props = {};

const Texteditor = (props: Props) => {
  return (
    <section className="h-full w-ful border-1 border-ring">Texteditor</section>
  );
};

export default Texteditor;
